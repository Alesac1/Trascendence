# Copy this file to .env and customize values for each environment

AUTH_HOST=0.0.0.0
AUTH_PORT=3005
# Directory Fastify uses for the SQLite database file
AUTH_DB_DIR=/app/database
# Optional: override the exact DB path (defaults to "$AUTH_DB_DIR/auth.db")
# AUTH_DB_PATH=/app/database/auth.db

# Replace with a strong, random secret in production
JWT_SECRET=please-change-me
