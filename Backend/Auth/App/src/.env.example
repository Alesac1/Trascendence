# TEMPLATE, copy and rename .env.example -> .env

AUTH_HOST=0.0.0.0
AUTH_PORT=3001
# Store the SQLite file relative to the project root, or set to ':memory:' for tests
AUTH_DB_PATH=./data/auth.db

# Replace with a strong, random secret in production
JWT_SECRET=please-change-me
